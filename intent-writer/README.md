# intent-writer

`intent-writer` generates a minimal `INTENT.md` by distilling the current session into:

- a one-sentence **Goal**
- a short list of **Target state (invariants)**

It is intentionally proposal-only: it prints the `INTENT.md` content instead of writing files.
