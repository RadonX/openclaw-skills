name: intent-writer
version: "0.1.0"
status: draft
owner: RadonX
created: "2026-03-18"
updated: "2026-03-18"

description: "Distill the current session into a minimal INTENT.md (Goal + Target state invariants)."

contract:
  - "Zero-arg: uses current session as sole input."
  - "Proposal-only: does not write files."
  - "Prefer not to ask questions; emit short error if session is insufficient."
  - "Output fixed to: header block (title/Model/Updated) + ## Goal + ## Target state (invariants) only."

