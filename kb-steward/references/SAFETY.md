# Safety rules

- Additive bootstrap only; never overwrite existing files.
- Read-before-write is mandatory for convention matching.
- Avoid deletion; prefer `.bak` or ask.
- Keep replies minimal; store suggestions inside notes.
