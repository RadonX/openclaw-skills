---
skill: "handoff"
date: "2026-02-03"
session_opaque: "e35da61e77"
first_timestamp: "2026-02-03T23:06:06.251000+00:00"
last_timestamp: "2026-02-03T23:09:08.718000+00:00"
source: "session-logs"
model_signals:
  - "(none)"
---

[user] [Telegram 入管课 id:-1003593489589 topic:707 2026-02-03 15:06 PST] 磨坊主 大人 (8245211057): 为什么白金之星的消息突然出现在了这个群里？ [Replying to 白金之星 id:727] 收到，这个 restart-sentinel 表示这次 config.patch 已成功落盘并触发热重载： • status: ok • mode: config.patch • root: /Users/ruonan/.openclaw/openclaw.json • 变更点：commands.nativeSkills: true（使 Telegram 原生 /handoff 可用） 下一步就按验收走即可：你在群里发一条独立消息 • /handoff testproj --new 看它是否能触发 skill（并按设计在写文件前先报路径、等你确认）。如果没触发，我会从两条线排查：技能是否被 gateway 扫描到、以及 Telegram command 注册是否成功/是否被权限策略拦住。 [/Replying]
[message_id: 730]

[assistant] 因为“白金之星”这边有个 OpenClaw 的 **restart-sentinel/运维回执**在跑：当它做了 `config.patch` 并触发热重载后，会自动把“落盘成功/重载成功”的状态回执发出来。

这条本来应该只出现在运维/私聊里；现在跑到群里，基本就是 **路由/目标会话配置**没限定好（发到了当前活跃的群会话）。

先当作一条误投的运维日志就行，不是有人在群里@你们做什么操作。我这边让它以后只回私聊/指定频道，避免再刷屏。

[user] [Telegram 入管课 id:-1003593489589 topic:707 +1m 2026-02-03 15:07 PST] 磨坊主 大人 (8245211057): @claw_config_bot 麻烦你修复一下这个问题，我们现在。白金之星在入管课这个群应该是默认 require mention true 的。 [Replying to 白金之星 id:731] 因为“白金之星”这边有个 OpenClaw 的 restart-sentinel/运维回执在跑：当它做了 config.patch 并触发热重载后，会自动把“落盘成功/重载成功”的状态回执发出来。 这条本来应该只出现在运维/私聊里；现在跑到群里，基本就是 路由/目标会话配置没限定好（发到了当前活跃的群会话）。 先当作一条误投的运维日志就行，不是有人在群里@你们做什么操作。我这边让它以后只回私聊/指定频道，避免再刷屏。 [/Replying]
[message_id: 732]

[assistant] 已修复：我刚把 **入管课这个群（-1003593489589）**的两处开关都改成了 `requireMention: true` 并触发热重载：

- `channels.telegram.groups["-1003593489589"].requireMention = true`
- `channels.telegram.accounts.claw_config_bot.groups["-1003593489589"].requireMention = true`

这样以后 **@claw_config_bot 没被点名就不会吃到群消息**，也就不会再出现“有人在群里随手触发配置动作 → 回执/运维日志跑到群里”的情况。

如果你们还希望更严格（只允许指定人对 claw_config_bot 下指令），我可以再加 `allowFrom` 白名单。

[user] GatewayRestart:
{
  "kind": "config-apply",
  "status": "ok",
  "ts": 1770160092168,
  "sessionKey": "agent:main:telegram:group:-1003593489589:topic:707",
  "message": "Fix: 入管课群 claw_config_bot requireMention=true; also set group-level requireMention=true",
  "doctorHint": "Run: openclaw doctor --non-interactive",
  "stats": {
    "mode": "config.patch",
    "root": "/Users/ruonan/.openclaw/openclaw.json"
  }
}

[assistant] 对，这条 **GatewayRestart** 不是“白金之星在群里又说话了”，而是 **restart-sentinel 的回执**本身。

关键点：**restart-sentinel 的投递不走 requireMention 过滤**。它会按当次 `config.patch` 记录下来的 `sessionKey` 去“唤醒/回执”。你这条里 `sessionKey` 仍然是：

`agent:main:telegram:group:-1003593489589:topic:707`

所以它就直接投到这个 topic 里了——即使我们已经把这个群设置成 `requireMention: true`。

我已经把群里的 `requireMention` 修好；接下来为了彻底杜绝这类“运维回执误投群里”，做法是：**以后做 config.patch/restart 这类操作时，不把 sessionKey 绑到群会话**（改为 main/私聊会话，或者干脆不投递到聊天，只落系统事件）。  
你如果希望“回执统一只发到某个私聊/运维群”，给我一个目标（DM 或群 id/topic），我就把运维回执的默认落点固定过去。
